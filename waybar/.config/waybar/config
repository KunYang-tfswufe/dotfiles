{
    "layer": "top",
    "position": "right",

    "modules-right": [
        "hyprland/workspaces", // <-- 新增: 在顶部添加工作区模块
        "tray"
    ],

    "tray": {
        "spacing": 10
    },

    // v-- 新增: 工作区模块的完整配置 --v
    "hyprland/workspaces": {
        "format": "{id}",
        "orientation": "vertical", // <-- 关键: 设置为垂直排列
        "on-click": "activate",
        "all-outputs": false,
        "show-special": false // 保持为 false 来隐藏 "magic" 工作区
    }
}
